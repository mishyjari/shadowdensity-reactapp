import {Container} from "react-bootstrap";

export const Blog = () => {
    return (
        <Container className={"red-border-dev body-content"}>
            <h1>Blog</h1>
        </Container>
    )
}