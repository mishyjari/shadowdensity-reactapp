import {Container} from "react-bootstrap";

export const About = () => {
    return (
        <Container className={"red-border-dev body-content"}>
            <h1>About</h1>
        </Container>
    )
}